# Core dependencies for the Telegram bot and web server
# StreamBot/requirements.txt
pyrofork
tgcrypto>=1.2.5
aiohttp>=3.9.0
aiohttp_cors>=0.7.0
python-dotenv>=1.0.0

# --- For-Database ------------ #
pymongo
dnspython

# --- Memory Management ------- #
psutil>=5.9.0

# --- Session Generator Web App --- #
aiohttp-jinja2>=1.5.0
Jinja2>=3.0.0
cryptography>=41.0.0

# --- URL Shortener ------------ #
# Using direct aiohttp requests (already included above)